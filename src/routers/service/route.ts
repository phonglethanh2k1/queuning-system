import { PATH_APP } from "../path";

export const ServiceRoute = {
  SERVICE: `${PATH_APP}/service`,
  ADD_SERVICE: `${PATH_APP}/add-service`,
  DETAIL_SERVICE: `${PATH_APP}/service/:id`,
  UPDATE_SERVICE: `${PATH_APP}/service/update/:id`,
};
