import { PATH_APP } from '../path';

export const LevelNoRoute = {
  LEVEL_NO: `${PATH_APP}/level-no`,
  NEW_NUMBER_LV: `${PATH_APP}/new-number-level`,
  DETAIL_LEVEL_NO: `${PATH_APP}/level-no/:id`,
  UPDATE_LEVEL_NO: `${PATH_APP}/level-no/update/:id`,
};
