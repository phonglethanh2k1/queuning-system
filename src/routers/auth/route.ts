import { PATH_AUTH } from '../path';

export const AuthRoute = {
  SIGN_IN: `${PATH_AUTH}/sign-in`,
  SIGN_UP: `${PATH_AUTH}/sign-up`,
  FORGOT_PASSWORD: `${PATH_AUTH}/forgot-password`,
  SIGN_OUT: `${PATH_AUTH}/sign-out`,
};
